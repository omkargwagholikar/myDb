use crate::{constants::*, cursor::Cursor, row::Row};

pub struct InternalNode{
    
}

impl InternalNode {
    pub fn initialize_internal_node(root: &mut Vec<u8>) {

    }
    
    pub fn internal_node_child(root: &mut Vec<u8>) {

    }
    pub fn internal_node_right_child(root: &mut Vec<u8>) {

    }
}